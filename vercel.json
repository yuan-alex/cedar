{
  "buildCommand": "prisma generate --no-engine --no-hints && prisma migrate deploy && vite build",
  "outputDirectory": "./dist/client",
  "installCommand": "bun install",
  "devCommand": "bun run dev",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
