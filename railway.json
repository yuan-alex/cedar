{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && bun run db:generate && bun run build"
  },
  "deploy": {
    "preDeployCommand": ["bun run db:generate && bun run db:migrate"],
    "startCommand": "bun run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
